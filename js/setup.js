/* Hooks React partagés entre tous les fichiers JSX.
   En classic-script scope, ces const sont visibles depuis tous les autres fichiers chargés après. */

const { useState, useEffect, useRef } = React;
